name: Security Actions

on: [push, pull_request]

jobs:
  install_n_gen:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.3.2
        with:
          fetch-depth: 0

      - name: Symfony Corp Security Checker
        uses: symfonycorp/security-checker-action@v2
