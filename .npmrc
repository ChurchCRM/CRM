# Enforce Node 24 (matches package.json engines, .nvmrc, and CI workflows)
engine-strict=true

# Suppress bootstrap-switch peer dep warning (AdminLTE expects Bootstrap 3, we use 4)
legacy-peer-deps=true

# To fix "Unknown env config devdir" warning, run: npm config delete devdir
